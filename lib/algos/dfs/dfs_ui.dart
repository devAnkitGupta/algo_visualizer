import 'package:flutter/material.dart';

class DfsVisualizer extends StatelessWidget {
  const DfsVisualizer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}